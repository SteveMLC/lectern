#!/bin/sh
set -eu

if [ "${AI_USAGE_SKIP:-0}" = "1" ] || [ ! -f usage/private/sources.json ]; then
  exit 0
fi

pnpm usage:sync -- --quiet
git add usage/ledger.jsonl usage/REPORT.md
if [ -f usage/receipts.jsonl ]; then
  git add usage/receipts.jsonl
fi
pnpm usage:check
